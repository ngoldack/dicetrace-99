{
  "name": "userdata",
  "scripts": {
    "dev": "cargo watch -x run",
    "build": "cargo build",
    "build:release": "cargo build --release",
    "lint": "cargo clippy -- -D warnings"
  },
  "dependencies": {
    "id-gen": "workspace:*",
    "rs-models": "workspace:*"
  }
}
